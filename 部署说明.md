# 快连下载 · Eleventy + Cloudflare Pages + Workers

GEMINI_API_KEY
Google AI Studio 申请的 API Key
INDEXNOW_KEY
必应 IndexNow 密钥（一串字母数字，例如 7ae66ba4241e87dd3a0f938d1ae621d0c3818237）
SITE_DOMAIN
你实际上线的域名，如 qhqrl.com（不要写 https://）


每次只推当天新发的文章，帮我加 PUSH_NEW_ONLY 模式

## 项目位置

`C:\Users\Administrator\Desktop\最新搭建网站\kuailian-site`

## 页面结构

| 路径 | 说明 |
|------|------|
| `/` | 首页（27 条内容卡片 + 四宫格下载入口 + 锚点板块） |
| `/blog/` | 博客列表（按目录分类 + 标签云） |
| `/download.html` | 下载中心（Windows / macOS / Android / iOS 四宫格） |
| `/posts/xxx/` | 文章页（上一篇 / 下一篇 + 相关阅读） |
| `/tags/标签名/` | 标签聚合页（自动生成） |

## 本地测试

```bash
cd kuailian-site
npm install
node scripts/seed-posts.mjs   # 首次生成 23 篇示例文章
npm run dev
```

浏览器打开：**http://localhost:8080/**

## 推送到 GitHub

```bash
git init
git add .
git commit -m "feat: 快连下载 Eleventy 站点"
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main
```

## Cloudflare Pages 设置

| 配置项 | 值 |
|--------|-----|
| Build command | `npm run build` |
| Build output | `_site` |
| Node | 20 |

## Gemini 自动发文 + 必应 IndexNow 推送

GitHub Secrets 需配置：

| Secret | 说明 |
|--------|------|
| `GEMINI_API_KEY` | Google Gemini API 密钥 |
| `INDEXNOW_KEY` | 必应 IndexNow 密钥（与 txt 文件名一致） |
| `SITE_DOMAIN` | 站点域名，如 `qhqrl.com`（不要带 https） |

流程（`.github/workflows/gemini-auto-content.yml`）：

1. 每天香港时间 **08:12** 自动生成 1 篇文章（cron `12 0 * * *` UTC）
2. 手动：**Actions → Gemini Auto Content Updater → Run workflow** → 输入 1–9 篇
3. 构建 Eleventy，将 `src/{INDEXNOW_KEY}.txt` 复制到 `_site`
4. 提交 `src/posts/`、`keywords.json`、验证 txt 到 GitHub
5. 等待线上验证文件 HTTP 200 后，**仅推送当天新发文 URL**（`PUSH_NEW_ONLY=1`，已默认开启）

### IndexNow 两种模式

| 模式 | 环境变量 | 行为 |
|------|----------|------|
| 仅新文（推荐/CI 默认） | `PUSH_NEW_ONLY=1` | 只推当天 `date:` 的新文章 |
| 全站 | 不设置 | 推送 `_site` 全部页面（约 60+ URL） |

识别当天新文的顺序：

1. `scripts/.indexnow-pending.json`（Gemini 生成后自动写入）
2. `src/posts/*.md` 里 `date:` 等于当天（香港时区）
3. Git 最近一次提交新增的 `src/posts/*.md`

本地手动测试：

```bash
set GEMINI_API_KEY=你的密钥
npm run generate

set SITE_DOMAIN=你的域名.com
set INDEXNOW_KEY=你的密钥
set PUSH_NEW_ONLY=1
npm run build
npm run bing:push
```

## Workers 部署（可选）

```bash
npm i -g wrangler
wrangler login
npm run worker:deploy
```

## 修改站点信息

编辑 `src/_data/site.json`：品牌名、TDK、导航、目录名称。

## 新增文章（手动）

在 `src/posts/` 新建 `.md`，示例：

```yaml
---
layout: layouts/post.njk
title: 标题须含快连下载
description: 摘要 80-120 字
category: 使用教程
tags:
  - 快连下载
  - Windows
date: 2026-05-23
permalink: /posts/your-slug/
---
```

图片格式：`![描述](https://tse-mm.bing.com/th?q=快连下载+关键词)`
